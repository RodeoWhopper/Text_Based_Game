package content;

public interface Entity {
    //This interface being used for common usage and code flexibility.

    interface ConcreteEntity extends Entity{
    }

    interface AbstractEntity extends Entity {
    }
}
